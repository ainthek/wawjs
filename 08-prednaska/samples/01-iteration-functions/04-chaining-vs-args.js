// Other language (functions only)

// oneliner
var output = map(map(map(input, f1), f2), f3);


// custom formatted
var output = map(
    map(
        map(
            input,
            f1
        ),
        f2
    ),
    f3
);